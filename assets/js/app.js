const products=[
 {title:'Long Box Finds',type:'Back issues · Salem shelves',price:'Ask at the counter',image:'assets/img/salem-shop-01.jpg'},
 {title:'Marvel & DC',type:'New arrivals · Superheroes',price:'Fresh every week',image:'assets/img/salem-shop-03.jpg'},
 {title:'Dungeons & Dragons',type:'Collectibles · Tabletop',price:'Curated picks',image:'assets/img/salem-shop-09.jpg'},
 {title:'Graphic Novels',type:'Collected editions · Staff picks',price:'Browse the shelf',image:'assets/img/salem-shop-11.jpg'}
];
const grid=document.querySelector('#productGrid');let bag=0;
products.forEach(p=>{const card=document.createElement('article');card.className='product-card';card.innerHTML=`<div class="product-art" style="background-image:url('${p.image}')"><strong>${p.title}</strong></div><div class="product-info"><div class="product-type">${p.type}</div><div class="product-title">${p.title}</div><div class="product-price">${p.price}</div></div><button class="add-button" aria-label="Save ${p.title} to bag">+</button>`;card.querySelector('button').addEventListener('click',()=>{bag++;document.querySelector('#bagCount').textContent=bag;card.querySelector('button').textContent='✓';setTimeout(()=>card.querySelector('button').textContent='+',900)});grid.appendChild(card)});
const menuButton=document.querySelector('#menuButton'),mobileMenu=document.querySelector('#mobileMenu');menuButton.addEventListener('click',()=>{const open=mobileMenu.classList.toggle('open');menuButton.setAttribute('aria-expanded',open)});mobileMenu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mobileMenu.classList.remove('open')));
const dialog=document.querySelector('#searchDialog'),input=document.querySelector('#searchInput'),result=document.querySelector('#searchResult');document.querySelector('#searchButton').addEventListener('click',()=>{dialog.hidden=false;input.focus()});document.querySelector('#closeSearch').addEventListener('click',()=>dialog.hidden=true);dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.hidden=true});document.querySelector('#searchForm').addEventListener('submit',e=>{e.preventDefault();result.textContent=input.value?`We'll help you find “${input.value}” — ask us at the counter.`:'Search our shelves.'});
